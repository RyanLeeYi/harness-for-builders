---
updated: YYYY-MM-DD
feature: <對應 feature_list.json 的 id 範圍,例:F1–F4>
---

# <功能名稱> — PRD

> **核心原則:harness 表現最好的時候,是它能自我驗證的時候。** 每條需求都要寫成「可執行、可判定對錯」的形式——模糊的「要有良好的錯誤處理」對 agent 沒用,「缺 email 欄位回傳 400 + `{error: "email required"}`」才有用。
> 一份 PRD 對應一個 feature 群;「需求與驗收標準」逐條轉成 `feature_list.json` 的 feature + acceptance。
> **開發規則不寫進 PRD**(不可變性、TDD、commit 格式放 `CLAUDE.md`)——重複放會漂移不同步,還吃 context。

## 背景與目標
<一兩句:為什麼現在做這個>

## 非目標(本次不做)
<比目標更重要——防 scope creep 最有效的工具>
- 不做 <X>
- 不碰 <既有的 Y 檔案 / API>

## 需求與驗收標準
<每條需求寫成可判定的形式,這是整份 PRD 對 harness 最關鍵的部分>
<標題用 user story(殼)保留角色與意圖;驗收用 Given/When/Then(核)可測>

### R1:作為<角色>,我想<動作>,以便<價值>
- Given <前置狀態>
- When <動作>
- Then <可觀察的結果,具體到能寫成測試斷言>

### R2:...

## 介面契約
<資料模型、型別、API endpoint 規格。先定死,agent 就不會自己發明不相容的結構>

## 具體範例(輸入 → 輸出)
<範例勝過千言萬語,給 2-3 個。request/response JSON、資料轉換前後>

## 邊界情況與錯誤行為
<沒寫的 agent 會自己猜>
- 空值 / 缺欄位:<行為>
- 重複提交:<行為>
- 外部服務失敗:<行為>

## 完成定義(必過的指令)
<給 harness 的回饋迴路:跑指令、看結果、自己修>
- `<測試指令>` 全過,覆蓋率 ≥ 80%
- `<lint 指令>` 無錯誤
- `<build 指令>` 成功

## 開放問題
<動工前清空,或標「遇到請停下來問」>
- [ ] <問題>(決策人:<誰>)
